/* app.js — legacy shim.
 *
 * The client was split into ES modules under assets/lib/ and assets/pages/
 * per PLAN.md Phase 6. This file remains as a no-op fallback in case an
 * older browser-cached page still references it directly. main.js is
 * loaded as the real entry point from views/layout.php.
 */
